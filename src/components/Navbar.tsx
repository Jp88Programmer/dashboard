import React from "react";

const Navbar = () => {
  return (
    <nav className="sticky p-4 flex items-center w-screen -z-10 text-2xl">
      <span className="text-3xl py-2">Economy Dashboard</span>
    </nav>
  );
};

export default Navbar;
